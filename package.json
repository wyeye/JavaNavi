{
  "name": "javanavi-desktop-workspace",
  "private": true,
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "verify:quick": "npm --prefix frontend test && npm --prefix frontend run build && npm run scan:source-health && cd backend && mvn -q test && cd ../src-tauri && mkdir -p resources/java-runtime && touch resources/javanavi-backend.jar && cargo check",
    "scan:source-health": "node scripts/scan-source-health.mjs && node frontend/scripts/check-i18n-message-literals.mjs && node scripts/check-backend-localized-errors.mjs",
    "desktop:stage": "./scripts/package-desktop-sidecar.sh",
    "desktop:dev": "npm run desktop:stage && tauri dev",
    "desktop:build": "npm run desktop:stage && tauri build",
    "desktop:build:linux": "npm run desktop:stage && tauri build --bundles deb rpm",
    "desktop:build:macos": "npm run desktop:stage && tauri build --bundles app",
    "desktop:build:windows": "npm run desktop:stage && tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
